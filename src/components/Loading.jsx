import React from 'react';

// Display a loading message centered on the screen
const Loading = () => {
    return (
        <div className="flex items-center justify-center min-h-screen text-center text-xl font-semibold">
            Loading...
        </div>
    );
};

export default Loading;
